name: CI

on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: Azure/powershell@v2
      - name: Task1
        with:          
          azPSVersion: latest
          inlineScript: echo Run task 1
      
